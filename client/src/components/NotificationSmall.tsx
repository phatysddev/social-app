import { JSX } from "react";

export default function NotificationSmall(): JSX.Element {
    return (
        <div className="w-full h-[50px] flex justify-start items-center overflow-hidden relative">
            
        </div>
    );
}